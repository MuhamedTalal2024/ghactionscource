on:
  workflow_dispatch:
    inputs:
      environment: 
      required: true
      default: "staging"
      type: choise
      options: 
       - staging 
       - production


jobs: 
  message:    
    runs-on: ubuntu-latest
    steps: 
      - name: echo message
        run: |
          echo "deploying to ${{ inputs.environment }}"
